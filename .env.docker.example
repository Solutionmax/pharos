# Copy to .env before running compose. APP_KEY is required; generate one with:
#   docker run --rm pharos:dev php artisan key:generate --show
APP_KEY=
APP_URL=http://localhost:8080
PHAROS_PORT=8080
PHAROS_LICENSE_PUBLIC_KEY=
